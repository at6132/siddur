/** Bundled in app via app.json → expo-notifications `sounds` (under 30s for iOS). */
export const SHABBOS_CLOCK_BUNDLE_SOUND = 'shabbos-alarm.wav';

export const SHABBOS_CLOCK_ANDROID_CHANNEL = 'shabbos_clock_alarm';

/** Expo `WEEKLY` trigger: 1 = Sunday … 7 = Saturday. */
export const EXPO_WEEKLY_SATURDAY = 7;
